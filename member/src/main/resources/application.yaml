server:
  port: 8081
spring:
  application:
    name: member

logging:
  level:
    com.szx.train: debug

knife4j:
  enable: true
  openapi:
    title: 会员接口文档
    description: 会员接口文档
    version: v1.0.0
    group:
      default:
        group-name: default
        api-rule: package
        api-rule-resources:
          - com.szx.train.member.controller
