spring:
  application:
    name: member
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        file-extension: yaml
        server-addr: 192.168.100.128:8848
        namespace: train
      discovery:
        server-addr: 192.168.100.128:8848
        namespace: train


test:
  spring: s

# seata配置
seata:
  tx-service-group: train-group
#  service:
#    vgroup-mapping:
#      train-group: default
#    grouplist:
#      default: 192.168.100.128:8099
  #seata注册中心
  registry:
    type: nacos
    nacos:
      server-addr: 192.168.100.128:8848
      application: seata-server
      group: SEATA_GROUP
      namespace: train
      username: nacos
      password: nacos
  #seata配置中心
  config:
    type: nacos
    nacos:
      server-addr: 192.168.100.128:8848
      data-id: seataServer.properties
      group: SEATA_GROUP
      namespace: train
      username: nacos
      password: nacos